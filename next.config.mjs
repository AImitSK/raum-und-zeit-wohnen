/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Alle Bilder liegen lokal in public/bilder – keine externen Remote-Patterns nötig.
};

export default nextConfig;
